# WebAssembly Assets

This directory contains the compiled WebAssembly (.wasm) files and their JavaScript glue code for the voting system.

These files are generated from the C source code in the `src/wasm` directory using Emscripten.