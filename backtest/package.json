{
    "name": "collector-backtest",
    "version": "1.0.0",
    "description": "Test backend server for the collector module",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}